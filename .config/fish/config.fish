abbr -a -- www 'python3 -m http.server'
abbr -a -- rsync 'rsync -aihz --exclude .DS_Store --stats'

abbr -a -- - 'cd -'

abbr -a -- l ls
abbr -a -- l1 'ls -1'
abbr -a -- la 'ls -A'
abbr -a -- ll 'ls -oh'
abbr -a -- lla 'ls -oAh'
